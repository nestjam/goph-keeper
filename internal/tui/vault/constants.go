package vault

const (
	baseURL      = "secrets"
	errTemplate  = "error: %s\n\n"
	codeTemplate = "code: %d\n\n"
	quitApp      = "quit"
	offlineMode  = "offline mode"
	noCachedData = "no cached data"
)
